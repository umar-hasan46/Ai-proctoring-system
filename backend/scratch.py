import os

with open('routes/interview_routes.py', 'r', encoding='utf-8') as f:
    content = f.read()

# I will write a custom python script to modify the file content safely or replace the entire file contents using multi_replace_file_content tool, but wait, the view_file tool already gave me the contents so I can just use replace_file_content.
